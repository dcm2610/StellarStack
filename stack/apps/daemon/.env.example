# Daemon configuration
DAEMON_HOST=0.0.0.0
DAEMON_PORT=3002

# Docker socket (optional, uses system default if not set)
# Linux: /var/run/docker.sock
# Windows: npipe:////./pipe/docker_engine
# DOCKER_SOCKET=/var/run/docker.sock

# ===========================================
# API Connection (required for panel sync)
# ===========================================
# URL of the StellarStack API
API_URL=http://localhost:3001

# Node token from the panel (get this when creating a node in Admin > Nodes)
# The daemon will use this token to authenticate with the API
DAEMON_TOKEN=f5e943fc1aab6f4e981347e8236492b74164092b628fb20d3fb965f1a4a61ebcecb347d37efcd16da5eeb34e0f4985eb

# Heartbeat interval in seconds (how often to ping the API)
HEARTBEAT_INTERVAL=30

# Logging
RUST_LOG=stellar_daemon=debug,tower_http=debug
