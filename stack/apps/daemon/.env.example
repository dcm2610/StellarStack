# Daemon configuration
DAEMON_HOST=0.0.0.0
DAEMON_PORT=3001

# Docker socket (optional, uses system default if not set)
# Linux: /var/run/docker.sock
# Windows: npipe:////./pipe/docker_engine
# DOCKER_SOCKET=/var/run/docker.sock

# Logging
RUST_LOG=stellar_daemon=debug,tower_http=debug
