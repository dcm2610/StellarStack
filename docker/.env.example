# ============================================
# StellarStack Docker Configuration
# ============================================
# This file contains all configuration needed to deploy StellarStack
# Copy this file to .env and fill in your values
# You can use the setup.sh script to generate this automatically

# ============================================
# DOMAINS & SSL
# ============================================
# Main panel domain (e.g., panel.yourcompany.com)
# This domain must point to your server's IP address
PANEL_DOMAIN=

# API domain (e.g., api.yourcompany.com)
# This domain must point to your server's IP address
API_DOMAIN=

# Admin email for Let's Encrypt notifications and certificate expiry warnings
ADMIN_EMAIL=

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL database name
POSTGRES_DB=stellar

# PostgreSQL username
POSTGRES_USER=stellar

# REQUIRED: Strong password for PostgreSQL
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=

# ============================================
# SECURITY & AUTHENTICATION
# ============================================
# CRITICAL: Secret for session signing and authentication
# This should be a long, random string (64+ characters)
# Generate with: openssl rand -base64 64
BETTER_AUTH_SECRET=

# Token secret for file downloads
# Generate with: openssl rand -base64 32
DOWNLOAD_TOKEN_SECRET=

# Encryption key for AES-256 encryption (must be exactly 32 bytes in hex format = 64 hex characters)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY=

# ============================================
# OAUTH PROVIDERS (Optional)
# ============================================
# GitHub OAuth Application
# Create at: https://github.com/settings/developers
# Callback URL: https://${API_DOMAIN}/api/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Google OAuth Application
# Create at: https://console.cloud.google.com/apis/credentials
# Callback URL: https://${API_DOMAIN}/api/auth/google/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Discord OAuth Application
# Create at: https://discord.com/developers/applications
# Callback URL: https://${API_DOMAIN}/api/auth/discord/callback
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# ============================================
# EMAIL CONFIGURATION (Optional)
# ============================================
# SMTP settings for email notifications (password resets, invitations, etc.)

# From email address
EMAIL_FROM=

# SMTP server hostname
EMAIL_HOST=

# SMTP server port (587 for STARTTLS, 465 for SSL/TLS)
EMAIL_PORT=587

# SMTP username
EMAIL_USER=

# SMTP password
EMAIL_PASSWORD=

# Use SSL/TLS connection (true/false)
EMAIL_SECURE=false

# ============================================
# BUILD CONFIGURATION
# ============================================
# Git commit hash for versioning (auto-detected during build)
# Leave empty to auto-detect, or set manually
GIT_COMMIT_HASH=

# ============================================
# ADVANCED SETTINGS (Optional)
# ============================================
# These settings have sensible defaults and typically don't need to be changed

# Database connection pool size (default: 10)
# DATABASE_POOL_SIZE=10

# API request timeout in milliseconds (default: 30000)
# API_TIMEOUT=30000

# Enable debug logging (true/false, default: false)
# DEBUG=false

# ============================================
# NOTES
# ============================================
# 1. Never commit .env files to version control
# 2. Keep your secrets secure and backed up separately
# 3. Rotate secrets regularly in production
# 4. Use strong, unique passwords for all services
# 5. Ensure both domains resolve to your server before running setup
# 6. Make sure ports 80 and 443 are accessible from the internet
# 7. For production, consider using a secrets management service
